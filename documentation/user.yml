/api/users/signin:
  post:
    tags:
      - User
    description: User sign in
    parameters:
      - name: email
        in: body
        type: string
        required: true
      - name: password
        in: body
        type: string
        required: true
    responses:
      200:
        description: Sign in successful
